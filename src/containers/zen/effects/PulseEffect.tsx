/**
 * Trivial zen mode — pulses an accent-colored CSS vignette via the
 * `.zen-pulse` class. All animation is handled in CSS.
 */
export function PulseEffect() {
  return <div className="zen-pulse" />;
}
